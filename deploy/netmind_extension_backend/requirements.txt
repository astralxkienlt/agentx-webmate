fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
# SQLAlchemy's async bridge is built on greenlet; it is not pulled in by the
# bare sqlalchemy wheel and every async query fails at runtime without it.
greenlet==3.1.1
asyncpg==0.30.0
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
# JWKS verification of Keycloak ID tokens.
python-jose[cryptography]==3.3.0
# AES-256-GCM envelope for stored model keys (also pulled in by python-jose).
cryptography==43.0.1
loguru==0.7.3
# Per-subject rate limiting on the provisioning routes.
slowapi==0.1.9
